name: Shell Commands
on:
  - push
  - workflow_dispatch

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: eccccho "HOLA Marco en Github Actions"
      - name: multiline script
        run: |
          node -v
          npm -v
